{
  "name": "hono-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@workers-powertools/hono": "workspace:*",
    "@workers-powertools/idempotency": "workspace:*",
    "@workers-powertools/logger": "workspace:*",
    "@workers-powertools/metrics": "workspace:*",
    "@workers-powertools/tracer": "workspace:*",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "catalog:",
    "@types/node": "^25.6.0",
    "esbuild": "^0.28.0",
    "vitest": "catalog:",
    "wrangler": "^4.87.0"
  }
}
